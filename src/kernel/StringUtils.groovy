package kernel

class StringUtils {

    static def tab(tabNumber = 1) {
        return "\t".repeat(tabNumber)
    }

    static def lineFeed(number = 1) {
        return "\n".repeat(number)
    }

}
